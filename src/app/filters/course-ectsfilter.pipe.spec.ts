import { CourseECTSFilterPipe } from './course-ectsfilter.pipe';

describe('CourseECTSFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseECTSFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
