import { CourseRatingFilterPipe } from './course-rating-filter.pipe';

describe('CourseRatingFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CourseRatingFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
